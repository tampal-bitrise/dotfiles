# Dotfiles symlinked

### Install with stow:

```stow --target ~/.config .```

### Homebrew installation:

# Leaving a machine
```brew leaves > leaves.txt```

# Fresh installation
```xargs brew install < leaves.txt```
